# TeeUnit Server Requirements
# For deployment (HuggingFace Spaces, Northflank, Docker)

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
websockets>=12.0
numpy>=1.24.0
httpx>=0.25.0

# For Teeworlds protocol (huffman, networking)
# No external dependencies needed - pure Python implementation
